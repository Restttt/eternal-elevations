import React from 'react';
import Typography from '@material-ui/core/Typography';

const Home = (props) => {
  return (
    <div>
      <Typography variant="h4">
        Hello
      </Typography>
    </div>
  )
};

export default Home;